<?xml version="1.0"?>
<!-- ============================================================
     Заставляем ClickHouse слушать на всех интерфейсах,
     а не только на 127.0.0.1 внутри контейнера.
     Без этого Prometheus и хост-машина не достучатся до :9363,
     :8123 и :9000.
     ============================================================ -->
<clickhouse>
    <listen_host>0.0.0.0</listen_host>
</clickhouse>
